# toDo
- [x] Оператор не должен видеть справочник фармацевты
- [ ] И команду выбрать из проводника в [[Загрузка расходных накладных | ЗАГРУЗКЕ НАКЛАДНЫХ]]
- [ ] Набор прав для ручного заполнения документов
- [ ] Набор прав для переоценки
- [ ] Нужно ли настройки привязать к пользователю? #Созвон 
- [ ] Какие роли нужны кроме админа и оператора? #Созвон 

# <font color="#938953">СПРАВОЧНИК ПОЛЬЗОВАТЕЛИ</font>
глФармацевт -  это глобальный фармацевт. Связан с пользователем, который логинится

 - [ ] Вход происходит через справочник фармацевты. Но это же имя используется и для справочника операторы, где хранятся настройки оператора. Делать так же? #Созвон . То есть ТекущийФармацевт и ТекущийПользователь - это разные справочники
 // Ввод пароля фармацевта
   	Если (ИмяПользователя()<>"Admin") И (ИмяПользователя()<>"Директор") И (ИмяПользователя()<>"Бухгалтер") Тогда 
	 	СпрФарм = СоздатьОбъект("Справочник.Фармацевты");  
		Пока (ПустоеЗначение(глФармацевт)=1) Цикл
			Пасс=0; ВвестиЧисло(Пасс, "Введите Ваш пароль:",4,0);
		    глФармацевт = ?(СпрФарм.НайтиПоКоду(Пасс)=1 ,СпрФарм.ТекущийЭлемент(), "");
		КонецЦикла;	
   	КонецЕсли;

	// Поиск текущего пользователя
	СпрЮзер = СоздатьОбъект("Справочник.Операторы");
	Если СпрЮзер.НайтиПоРеквизиту("Пользователь", ИмяПользователя(), 1)=1 Тогда 
		глЮзер = СпрЮзер.ТекущийЭлемент();
		
		// Устанавливаем тип драйвера по-умолчанию из костанты, если он опущен в справочнике операторов
		Если ПустоеЗначение(глЮзер.ТипДрайвераКА)=1 Тогда глЮзер.ТипДрайвераКА = Константа.ТипДрайвера; КонецЕсли;
		Если ПустоеЗначение(глЮзер.ПортКА)=1 		Тогда глЮзер.ПортКА = "COM1"; 							КонецЕсли;
		Если ПустоеЗначение(глЮзер.СкоростьКА)=1 	Тогда глЮзер.СкоростьКА = 9600; 					КонецЕсли;
		
		// Открываем кассе при загрузки
		Если (глЮзер.ОткрКассу=глИстина) Тогда ОткрытьФорму("Обработка.Касса"); КонецЕсли;
		
	Иначе Сообщить("Пользователь: "+ИмяПользователя()+" не найден!!")
	КонецЕсли;

# <font color="#938953">РОЛИ АДМИНИСТРАТОР и ОПЕРАТОР</font> 

# <font color="#938953">ПОЛЬЗОВАТЕЛИ</font>
- [x] Администрирование
- [x] Справочник Пользователи
- [x] Добавлять пользователей из предприятия
- [x] Просмотр списка пользователей
- [x] Изменение реквизитов
- [x] Выровнять элементы на форме
- [x] ФОРМА СПИСКА